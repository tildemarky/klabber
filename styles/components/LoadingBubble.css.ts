import { style } from "@vanilla-extract/css";

export const StyledLoadingBubble = style({
  minWidth: "3.5rem",
  minHeight: "1.5rem",
  padding: "0.5rem 2rem",
  backgroundPosition: "center",
  backgroundRepeat: "no-repeat",
  backgroundSize: "40%",
});
