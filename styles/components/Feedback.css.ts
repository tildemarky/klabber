import { style } from "@vanilla-extract/css";

export const FeedbackGroup = style({
  display: "inline-flex",
  alignSelf: "flex-start",
});
export const FeedbackText = style({
  margin: "0.5rem 1rem",
});
