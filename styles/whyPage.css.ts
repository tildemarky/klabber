import { style } from "@vanilla-extract/css";

export const StyledPage = style({
  margin: "0 auto",
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  minHeight: "60vh",
  width: "75vw",
});
