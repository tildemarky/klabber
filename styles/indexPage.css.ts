import { style } from "@vanilla-extract/css";

const MessageBubbles = style({
  marginTop: "1rem",
  marginBottom: "1rem",
  display: "flex",
  flexDirection: "column",
  gap: "0.5rem",
});

export default MessageBubbles;
