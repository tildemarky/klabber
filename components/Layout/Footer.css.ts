import { style } from "@vanilla-extract/css";

const StyledFooter = style({
  flex: "0",
  width: "100%",
  textAlign: "center",
  marginTop: "3rem",
  paddingBottom: "2rem",
});

export default StyledFooter;
